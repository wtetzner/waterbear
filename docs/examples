
module org.bovinegenius.wombat/Main

use lang.wombat/Vmu

main : () -> ()
main() {
  let x: u8 = sum(4, 5);
  let y: u8 = 7:u8;
}

// Add two unsigned 8-bit ints
sum : (u8, u8) -> u8
sum(left, right) = left + right

